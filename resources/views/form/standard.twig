{{ asset_add("scripts.js", "root::js/form/translations.js") }}

{% block content %}

    {{ form_open({'class': 'form', 'enctype': 'multipart/form-data', 'url': form.options.url ?: url_full()}) }}

    {{ view("form/partials/heading", {'form': form}) }}

    {{ view("form/partials/layout", {'form': form}) }}

    {{ view("buttons/buttons", {'buttons': form.actions}) }}

    {% if form.buttons.count() %}
        {{ view("buttons/buttons", {'buttons': form.buttons}) }}
    {% endif %}

    {{ form_close() }}

{% endblock %}
